spring:
  application:
    name: stick-upload-service
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_HOST:localhost}:8848
        namespace: ${NACOS_NAMESPACE:dev}
        group: ${NACOS_GROUP:UPLOAD_GROUP}
      config:
        server-addr: ${NACOS_HOST:localhost}:8848
        namespace: ${NACOS_NAMESPACE:dev}
        group: ${NACOS_GROUP:UPLOAD_GROUP}
        file-extension: yaml
