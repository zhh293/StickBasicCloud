spring:
  application:
    name: stick-search-service
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_HOST:localhost}:8848
        namespace: ${NACOS_NAMESPACE:dev}
        group: ${NACOS_GROUP:TOTAL_GROUP}
      config:
        server-addr: ${NACOS_HOST:localhost}:8848
        namespace: ${NACOS_NAMESPACE:dev}
        group: ${NACOS_GROUP:SEARCH_GROUP}
        file-extension: yaml
