spring:
  application:
    name: stick-stick-service
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_HOST:localhost}:8848
        namespace: ${NACOS_NAMESPACE:dev}
        group: ${NACOS_GROUP:TOTAL_GROUP}
      config:
        server-addr: ${NACOS_HOST:localhost}:8848
        namespace: ${NACOS_NAMESPACE:dev}
        group: ${NACOS_GROUP:STICK_GROUP}
        file-extension: yaml
