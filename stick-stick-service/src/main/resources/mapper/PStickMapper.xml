<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tmd.stick.mapper.PStickMapper">
    <select id="pStickList" resultType="com.tmd.common.entity.dto.PStickVO">
        select s.id, s.name, s.content, s.created_at as createdAt, s.updated_at as updatedAt from psticks s
        <where>
            user_id = #{userId}
            <if test="name != null and name != ''">
                and name like concat('%',#{name},'%')
            </if>
            <if test="content != null and content != ''">
                and content like concat('%',#{content},'%')
            </if>
            <if test="startDate != null">
                and s.created_at >= #{startDate}
            </if>
            <if test="endDate != null">
                and s.created_at &lt;= #{endDate}
            </if>
        </where>
    </select>

    <update id="updatePStick">
        update psticks
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="content != null and content != ''">
                content = #{content},
            </if>
            <if test="(name != null and name != '') or (content != null and content != '')">
                updated_at = CURRENT_TIMESTAMP
            </if>
        </set>
        where id = #{pStickId}
    </update>
</mapper>