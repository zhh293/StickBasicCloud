<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tmd.content.mapper.MailCommentMapper">
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into sticknew.mail_comment
        (mail_id, commenter_id, content, created_at) VALUES
                                                         (#{mailId}, #{commenterId}, #{content}, #{createdAt})
    </insert>
</mapper>