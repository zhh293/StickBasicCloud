spring:
  application:
    name: stick-user-service
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_HOST:localhost}:8848
        namespace: ${NACOS_NAMESPACE:dev}
        group: ${NACOS_GROUP:TOTAL_GROUP}
      config:
        server-addr: ${NACOS_HOST:localhost}:8848
        namespace: ${NACOS_NAMESPACE:dev}
        group: ${NACOS_GROUP:USER_SERVICE_GROUP}
        file-extension: yaml
