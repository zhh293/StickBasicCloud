spring:
  application:
    name: stick-divination-service
  cloud:
    nacos:
      discovery:
        server-addr: ${NACOS_HOST:localhost}:8848
        namespace: ${NACOS_NAMESPACE:dev}
        group: ${NACOS_GROUP:QIAN_GROUP}
      config:
        server-addr: ${NACOS_HOST:localhost}:8848
        namespace: ${NACOS_NAMESPACE:dev}
        group: ${NACOS_GROUP:QIAN_GROUP}
        file-extension: yaml
